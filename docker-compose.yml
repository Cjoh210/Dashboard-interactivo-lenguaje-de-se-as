services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./videos:/app/videos   # montar carpeta de videos del host
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
